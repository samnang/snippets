<h1>Snippets</h1>
<%= link_to 'Create Snippet', new_snippet_path %>

<table class="snippets">
  <tbody>
    <% @snippets.each do |snippet| %>
      <tr><td><%= link_to snippet_summary(snippet), snippet %></td></tr>
    <% end %>
  </tbody>
</table>

<%= paginate @snippets %>
